name: qersheen
description: "Qersheen - Egyptian Finance & Wallet Manager"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  fl_chart: ^0.66.0
  permission_handler: ^11.3.0
  flutter_sms_inbox: ^1.0.3
  notification_listener_service: ^0.3.0
  local_auth: ^3.0.2
  file_picker: ^12.2.0
  share_plus: ^13.3.0
  pdf: ^3.13.0
  printing: ^5.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logos/
